<UserControl x:Class="ResourceGrab.App.Views.ReaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             PreviewKeyDown="ReaderView_PreviewKeyDown">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 顶部工具栏 · 全新胶囊式设计（按 / 或右上浮钮隐藏/显示） -->
        <Border x:Name="TopToolbar" Grid.Row="0" Margin="0,0,0,12"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1"
                CornerRadius="{DynamicResource RadiusXl}" Padding="12"
                SnapsToDevicePixels="True">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- 第一行：返回 + 标题 / 分页 + 章节 -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button x:Name="BackButton" Style="{StaticResource GhostButtonStyle}" Padding="10,6" Click="BackButton_Click" ToolTip="返回">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Path Data="{StaticResource IconBack}" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.8" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" Stretch="Uniform" Width="14" Height="14" VerticalAlignment="Center" />
                                <TextBlock Text="返回" Margin="6,0,0,0" VerticalAlignment="Center" FontWeight="SemiBold" />
                            </StackPanel>
                        </Button>
                        <StackPanel Margin="12,0,0,0" VerticalAlignment="Center" MaxWidth="420">
                            <TextBlock x:Name="TitleText" FontSize="14" FontWeight="Bold" TextTrimming="CharacterEllipsis" MaxWidth="420" />
                            <Border CornerRadius="{DynamicResource RadiusMd}" Background="{DynamicResource PrimarySubtleBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" Padding="8,3" Margin="0,5,0,0" HorizontalAlignment="Left" SnapsToDevicePixels="True">
                                <TextBlock x:Name="PageText" FontSize="12.5" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}" VerticalAlignment="Center" />
                            </Border>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- 页码胶囊 -->
                        <Border Background="{DynamicResource AppBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="{DynamicResource RadiusMd}" Padding="3" VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Button x:Name="PrevPageButton" Style="{StaticResource GhostButtonStyle}" Padding="10,6" Click="PrevPage_Click" ToolTip="上一页">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Path Data="{StaticResource IconChevronLeft}" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.6" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Stretch="Uniform" Width="12" Height="12" VerticalAlignment="Center" />
                                        <TextBlock Text="上一页" Margin="4,0,0,0" VerticalAlignment="Center" />
                                    </StackPanel>
                                </Button>
                                <Border Width="1" Background="{DynamicResource DividerBrush}" Margin="4,3" />
                                <Button x:Name="NextPageButton" Style="{StaticResource GhostButtonStyle}" Padding="10,6" Click="NextPage_Click" ToolTip="下一页">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="下一页" VerticalAlignment="Center" />
                                        <Path Data="{StaticResource IconChevronRight}" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.6" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Stretch="Uniform" Width="12" Height="12" Margin="4,0,0,0" VerticalAlignment="Center" />
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </Border>
                        <!-- 跳转胶囊 -->
                        <Border Background="{DynamicResource AppBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="{DynamicResource RadiusMd}" Padding="4" Margin="8,0,0,0" VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBox x:Name="PageJumpBox" Width="52" Height="24" VerticalContentAlignment="Center" FontSize="12" Padding="6,0" ToolTip="输入页码后回车或点击跳转" KeyDown="PageJumpBox_KeyDown" PreviewTextInput="PageJumpBox_PreviewTextInput" Style="{StaticResource ModernTextBoxStyle}" />
                                <TextBlock Text="/" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="6,0,0,0" FontSize="12" />
                                <TextBlock x:Name="JumpTotalText" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="4,0,0,0" FontSize="12" MinWidth="24" />
                                <Button x:Name="PageJumpButton" Style="{StaticResource GhostButtonStyle}" Content="跳转" Margin="6,0,0,0" Padding="10,5" Click="PageJump_Click" ToolTip="跳转到指定页" />
                            </StackPanel>
                        </Border>
                        <!-- 章节胶囊 -->
                        <Border Background="{DynamicResource AppBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="{DynamicResource RadiusMd}" Padding="3" Margin="8,0,0,0" VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Button x:Name="PrevChapterButton" Style="{StaticResource GhostButtonStyle}" Padding="10,6" Click="PrevChapter_Click" ToolTip="上一章">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Path Data="{StaticResource IconChevronLeft}" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.6" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Stretch="Uniform" Width="12" Height="12" VerticalAlignment="Center" />
                                        <TextBlock Text="上一章" Margin="4,0,0,0" VerticalAlignment="Center" />
                                    </StackPanel>
                                </Button>
                                <ComboBox x:Name="ChapterCombo" Width="148" Margin="6,0" VerticalAlignment="Center" SelectionChanged="ChapterCombo_SelectionChanged" />
                                <Button x:Name="NextChapterButton" Style="{StaticResource GhostButtonStyle}" Content="下一章" Padding="10,6" Click="NextChapter_Click" />
                            </StackPanel>
                        </Border>
                        <!-- 隐藏顶部工具栏：与找回浮钮同图标同语义 -->
                        <Button x:Name="HideToolbarButton" Style="{StaticResource GhostButtonStyle}"
                                Padding="10,6" Margin="8,0,0,0" VerticalAlignment="Center"
                                Click="HideToolbarButton_Click" ToolTip="隐藏顶部工具栏（/）">
                            <Path Data="{StaticResource IconPanelToggle}"
                                  Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                  StrokeThickness="1.6"
                                  StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round"
                                  Stretch="Uniform" Width="15" Height="15"
                                  HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Button>
                    </StackPanel>
                </Grid>

                <Border Grid.Row="1" Height="1" Background="{DynamicResource DividerBrush}" Margin="0,10,0,10" Opacity="0.8" />

                <!-- 第二行：模式 + 适应 + 速度 -->
                <DockPanel Grid.Row="2" LastChildFill="False">
                    <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Background="{DynamicResource AppBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="{DynamicResource RadiusMd}" Padding="3" VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ToggleButton x:Name="ScrollModeButton" Style="{StaticResource ReaderModeToggleStyle}" Content="滚动" Padding="14,0" MinWidth="56" Click="ModeToggle_Click" />
                                <ToggleButton x:Name="PageModeButton" Style="{StaticResource ReaderModeToggleStyle}" Content="翻页" Margin="3,0,0,0" IsChecked="True" Padding="14,0" MinWidth="56" Click="ModeToggle_Click" />
                            </StackPanel>
                        </Border>
                        <Border Background="{DynamicResource HoverBgBrush}" CornerRadius="{DynamicResource RadiusMd}" Padding="9,5" Margin="10,0,0,0" VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="/ → 隐藏顶部" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" />
                                <Border Width="1" Height="12" Background="{DynamicResource DividerBrush}" Margin="8,0" VerticalAlignment="Center" />
                                <TextBlock x:Name="ModeHintText" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" Text="缩放：滑杆/± 或 Ctrl + 滚轮" />
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Background="{DynamicResource AppBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="{DynamicResource RadiusMd}" Padding="3" VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Button x:Name="FitHeightButton" Style="{StaticResource GhostButtonStyle}" Content="适应高度" Padding="10,5" Click="FitHeight_Click" />
                                <Button x:Name="FitWidthButton" Style="{StaticResource GhostButtonStyle}" Content="适应宽度" Margin="3,0,0,0" Padding="10,5" Click="FitWidth_Click" ToolTip="条漫推荐：滚动模式 + 适应宽度" />
                                <Button x:Name="FitPageButton" Style="{StaticResource GhostButtonStyle}" Content="适应画面" Margin="3,0,0,0" Padding="10,5" Click="FitPage_Click" />
                                <Button x:Name="ActualSizeButton" Style="{StaticResource GhostButtonStyle}" Content="实际大小" Margin="3,0,0,0" Padding="10,5" Click="ActualSize_Click" />
                            </StackPanel>
                        </Border>
                        <!-- 缩放控制：条漫时拖动滑杆/±按钮控制图片宽度，百分比点击复位 -->
                        <Border Background="{DynamicResource AppBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="{DynamicResource RadiusMd}" Padding="3" Margin="8,0,0,0" VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Button x:Name="ZoomOutButton" Style="{StaticResource GhostButtonStyle}" Content="−" Padding="10,5" Click="ZoomOut_Click" ToolTip="缩小 (Ctrl + -)" FontWeight="Bold" />
                                <Slider x:Name="ZoomSlider" Width="110" Minimum="0.5" Maximum="3.0" TickFrequency="0.05" IsSnapToTickEnabled="False" VerticalAlignment="Center" Margin="6,0,0,0" Value="1.0" ValueChanged="ZoomSlider_ValueChanged" ToolTip="拖动缩放图片（50% ~ 300%），条漫建议 100% ~ 200%" />
                                <Button x:Name="ZoomResetButton" Style="{StaticResource GhostButtonStyle}" Content="100%" Margin="6,0,0,0" Padding="8,5" MinWidth="52" Click="ZoomReset_Click" ToolTip="点击恢复 100% (Ctrl + 0)" FontWeight="SemiBold" />
                                <Button x:Name="ZoomInButton" Style="{StaticResource GhostButtonStyle}" Content="+" Margin="3,0,0,0" Padding="10,5" Click="ZoomIn_Click" ToolTip="放大 (Ctrl + +)" FontWeight="Bold" />
                            </StackPanel>
                        </Border>
                        <Border Background="{DynamicResource AppBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="{DynamicResource RadiusMd}" Padding="10,5" Margin="8,0,0,0" VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="滚动速度" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" />
                                <Slider x:Name="ScrollSpeedSlider" Width="96" Minimum="0.3" Maximum="3.0" TickFrequency="0.1" IsSnapToTickEnabled="True" VerticalAlignment="Center" Margin="8,0,0,0" ValueChanged="ScrollSpeedSlider_ValueChanged" ToolTip="仅滚动模式生效" />
                                <TextBlock x:Name="ScrollSpeedValueText" FontSize="11" Foreground="{DynamicResource PrimaryBrush}" FontWeight="SemiBold" VerticalAlignment="Center" Margin="8,0,0,0" MinWidth="28" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </DockPanel>
            </Grid>
        </Border>

        <!-- 阅读区 -->
        <Border Grid.Row="1" Background="{DynamicResource HoverBgBrush}" CornerRadius="{DynamicResource RadiusXl}" Padding="8">
            <Grid>
                <ScrollViewer x:Name="Scroller" VerticalScrollBarVisibility="Hidden"
                              HorizontalScrollBarVisibility="Auto"
                              ScrollChanged="Scroller_ScrollChanged"
                              SizeChanged="Scroller_SizeChanged"
                              PreviewMouseWheel="Scroller_PreviewMouseWheel">
                    <StackPanel x:Name="ImageStack" HorizontalAlignment="Center" />
                </ScrollViewer>
                <!-- 工具栏隐藏时的找回浮钮：圆形悬浮按钮 + 矢量图标，半透明不挡画面 -->
                <Button x:Name="ShowToolbarButton"
                        Width="40" Height="40" Padding="0"
                        HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,8,8,0"
                        Opacity="0.85" Visibility="Collapsed"
                        Click="ShowToolbarButton_Click" ToolTip="显示顶部工具栏（/）">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource GhostButtonStyle}">
                            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource CardBorderBrush}" />
                            <Setter Property="BorderThickness" Value="1" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="20"
                                                SnapsToDevicePixels="True">
                                            <Path Data="{StaticResource IconPanelToggle}"
                                                  Stroke="{TemplateBinding Foreground}"
                                                  StrokeThickness="1.6"
                                                  StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round"
                                                  Stretch="Uniform" Width="18" Height="18"
                                                  HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Opacity" Value="1" />
                                                <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}" />
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter Property="Opacity" Value="1" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>

