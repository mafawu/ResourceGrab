<UserControl x:Class="ResourceGrab.App.Controls.FilterSidebar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="FilterChipStyle" TargetType="ToggleButton">
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Padding" Value="8,3" />
            <Setter Property="Margin" Value="0,0,4,4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="ChipBorder"
                                CornerRadius="{DynamicResource RadiusSm}"
                                Background="{DynamicResource HoverBgBrush}"
                                BorderBrush="{DynamicResource DividerBrush}"
                                BorderThickness="1" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ChipBorder" Property="Background"
                                        Value="{DynamicResource PrimarySubtleBrush}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="ChipBorder" Property="Background"
                                        Value="{DynamicResource PrimaryBrush}" />
                                <Setter TargetName="ChipBorder" Property="BorderBrush"
                                        Value="{DynamicResource PrimaryBrush}" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <!-- 搜索框 -->
        <TextBox x:Name="KeywordBox" Margin="{DynamicResource SpacingMd}"
                 Style="{StaticResource SearchBoxStyle}"
                 TextChanged="Keyword_Changed"
                 ToolTip="输入关键词搜索" />

        <!-- 分组列表 -->
        <ItemsControl x:Name="SectionsHost">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="{DynamicResource SpacingMd}">
                        <Grid Margin="0,0,0,6">
                            <TextBlock Text="{Binding Title}" FontWeight="SemiBold"
                                       FontSize="{DynamicResource FontBody}"
                                       Foreground="{DynamicResource TextPrimaryBrush}" />
                        </Grid>
                        <ItemsControl ItemsSource="{Binding Options}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <ToggleButton Style="{StaticResource FilterChipStyle}"
                                                  Content="{Binding Label}"
                                                  IsChecked="{Binding State, Converter={x:Null}}"
                                                  Click="Chip_Click"
                                                  Tag="{Binding}" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- 清除全部 -->
        <Button x:Name="ClearButton" Content="清除筛选" Click="Clear_Click"
                Margin="{DynamicResource SpacingMd}" Cursor="Hand" />
    </StackPanel>
</UserControl>
